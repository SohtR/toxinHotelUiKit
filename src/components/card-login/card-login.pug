.card.loginAccount
    h1 Войти
    +textField(data.cardLogin.emailField.id, data.cardLogin.emailField.placeholder, data.cardLogin.emailField.type)
    +textField(data.cardLogin.passField.id, data.cardLogin.passField.placeholder, data.cardLogin.passField.type)
    +button(data.cardLogin.btnLongStyle, data.cardLogin.btnLongContent)
    .flex
        p.haveAccount Нет аккаунта на Toxin?
        +button(data.cardLogin.btnBorderStyle, data.cardLogin.btnBorderContent)